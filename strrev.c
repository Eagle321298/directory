#include <stdio.h>
#include <string.h>
int main()
{
    char s1[] = {"Rutika "};
    char s2[] = {"bhuimbar"};
    printf("the reversed of s1 is %s\n", strrev(s1));
    printf("the reversed of s2 is %s\n", strrev(s2));

    return 0;
}